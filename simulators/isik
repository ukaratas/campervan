#!/bin/bash
# Işık kontrol script'i - venv otomatik aktive eder

SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd "$SCRIPT_DIR"

# Virtual environment'ı aktive et
source venv/bin/activate

# Komutu çalıştır
python3 kontrol.py "$@"

